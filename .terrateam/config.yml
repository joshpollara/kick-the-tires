# Repro for empty tree_builder output (Avaloq / Livia report).
# The tree builder emits zero files, which used to be indistinguishable
# from "tree not built" and produced EXPECTED_BUILT_REPO_TREE.
tree_builder:
  enabled: true
  script: |
    echo '{"files": []}'
